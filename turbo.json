{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "build/**"],
      "outputLogs": "new-only"
    },
    "lint": {
      "outputs": []
    },
    "release": {
      "dependsOn": [""]
    },
    "dev": {
      "cache": false,
      "dependsOn": ["^build"]
    }
  }
}
